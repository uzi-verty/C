Write a program to copy its input to its output, replacing each tab by **\t**, each backspace by **\b**, and each backslash by **\b**. This makes the tabs and backspaces visible in an unambigous way.
